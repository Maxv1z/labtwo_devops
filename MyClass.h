#ifndef MYCLASS_H
#define MYCLASS_H

class MyClass {
public:
   /**
     * Calculates the sum of the first `n` elements of the sequence.
     * @param x - initial value to start calculation
     * @param n - number of elements to sum
     */
     double FuncA(double x, int n);
};

#endif
